# CHASM 论文复现依赖
# Python >= 3.10

# 核心
datasets>=2.18.0
huggingface_hub>=0.23.0
numpy>=1.24.0
scikit-learn>=1.3.0
pandas>=2.0.0
pyyaml>=6.0
joblib>=1.3.0
tqdm>=4.66.0

# API 推理 (Table 2 闭源模型)
openai>=1.30.0

# 本地 MLLM 推理与微调 (Table 3)
torch>=2.1.0
transformers>=4.45.0
accelerate>=0.30.0
peft>=0.11.0
bitsandbytes>=0.43.0
pillow>=10.0.0

# 可选：多模态基线 BERT+ResNet, CLIP (Table 7)
# torchvision>=0.16.0
# sentence-transformers>=2.7.0

# 可选：ms-swift 微调框架
# ms-swift>=2.0.0

# 可视化
matplotlib>=3.8.0
